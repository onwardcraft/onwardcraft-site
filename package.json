{
  "name": "onwardcraft-site",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && cp dist/sitemap-0.xml dist/sitemap.xml",
    "preview": "astro preview",
    "deploy": "wrangler pages deploy ./dist"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.3.7"
  },
  "devDependencies": {
    "marked": "^18.0.5",
    "playwright": "^1.60.0"
  }
}
